# Runtime dependencies (CPU-only, no API keys required).
# sentence-transformers pulls in transformers + torch (CPU wheels on Windows).
# It also provides CrossEncoder, used by the optional --rerank flag.
sentence-transformers>=3.0